<UserControl x:Class="StreamControl.Views.StartupDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
             prism:ViewModelLocator.AutoWireViewModel="True" FocusManager.FocusedElement="{Binding ElementName=txt}">
    <StackPanel Margin="4">
        <ItemsControl ItemsSource="{Binding Lowerthirds}" Focusable="False">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="0,4,0,0">
                        <TextBlock Text="{Binding Title}"/>
                        <TextBox Text="{Binding Text}" x:Name="txt"/>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Button Content="{Binding Conf.SaveText}" Command="{Binding OkCommand}" Margin="0,8,0,4" IsDefault="True"/>
    </StackPanel>
    
</UserControl>
